#include "phantomcore/ring_buffer.hpp"

// Ring buffer is header-only, but we need this for the library to link properly
namespace phantomcore {
    // Explicit template instantiations if needed
}
